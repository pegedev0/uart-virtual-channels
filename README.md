# uart-virtual-channels
Implementación de un protocolo de multiplexación sobre UART que permite la creación de múltiples canales virtuales independientes. Diseñado para sistemas empotrados, permite separar flujos de telemetría, comandos críticos y logs ASCII en una única interfaz física mediante un sistema de prioridades, byte-stuffing y control de integridad CRC.
